{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"tiledpy","text":"<p>A Python library for loading Tiled <code>.tmx</code> maps, using Pillow for sprite detection and Pygame for hardware-accelerated rendering with cached surfaces.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Parse <code>.tmx</code> files (finite and infinite/chunked maps)</li> <li>Load external <code>.tsx</code> tilesets or inline definitions</li> <li>Encodings: CSV, Base64 + zlib / gzip / zstd</li> <li>Tile flip and rotation flags (<code>GID_FLIP_H/V/D</code>)</li> <li>Per-tile properties, collision objects, and animations</li> <li>Pillow-based sprite helpers: empty detection, dominant color</li> <li>Pygame <code>draw_layer()</code> with two-level surface cache (tile + scaled)</li> <li>Viewport culling \u2014 only draws tiles inside the screen</li> </ul>"},{"location":"#quick-install","title":"Quick install","text":"<pre><code>pip install -e \".[docs]\"   # with docs extras\npip install -e .           # runtime only\n</code></pre>"},{"location":"#minimal-usage","title":"Minimal usage","text":"<pre><code>import pygame\nfrom tiledpy import TiledMap\n\npygame.init()\nscreen = pygame.display.set_mode((800, 600))\nclock  = pygame.time.Clock()\n\ntmap = TiledMap(\"map.tmx\")\ncam_x, cam_y = 0, 0\n\nrunning = True\nwhile running:\n    for event in pygame.event.get():\n        if event.type == pygame.QUIT:\n            running = False\n\n    screen.fill((0, 0, 0))\n    tmap.draw_all_layers(screen, offset=(cam_x, cam_y))\n    pygame.display.flip()\n    clock.tick(60)\n</code></pre>"},{"location":"#high-level-loading-flow","title":"High-level loading flow","text":"<pre><code>flowchart TD\n    A([TiledMap.__init__]) --&gt; B[Parse &amp;lt;map&amp;gt; attributes]\n    B --&gt; C{Has tilesets?}\n    C -- inline --&gt; D[Parse inline tileset]\n    C -- .tsx source --&gt; E[Parse external TSX file]\n    D --&gt; F[Open spritesheet\\nwith Pillow]\n    E --&gt; F\n    F --&gt; G[Tileset ready\\nfirstgid \u00b7 columns \u00b7 tilecount]\n    G --&gt; H{More layers?}\n    H -- tilelayer --&gt; I[Decode data\\nCSV / Base64]\n    I --&gt; J{Infinite map?}\n    J -- yes --&gt; K[load_from_chunks]\n    J -- no --&gt; L[load_from_flat]\n    K --&gt; M[TileLayer ready]\n    L --&gt; M\n    H -- objectgroup --&gt; N[Parse TileObject list]\n    N --&gt; O[ObjectLayer ready]\n    M --&gt; P{More layers?}\n    O --&gt; P\n    P -- yes --&gt; H\n    P -- no --&gt; Q([TiledMap ready])</code></pre>"},{"location":"#package-structure","title":"Package structure","text":"<pre><code>tiledpy/\n\u251c\u2500\u2500 __init__.py      Public API exports\n\u251c\u2500\u2500 loader.py        TiledMap \u2014 parser + draw_layer()\n\u251c\u2500\u2500 tileset.py       Tileset \u2014 Pillow crop + pygame surface cache\n\u251c\u2500\u2500 layer.py         TileLayer \u00b7 ObjectLayer \u00b7 TileObject\n\u2514\u2500\u2500 renderer.py      draw_layer() with two-level global cache\n</code></pre>"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#class-diagram","title":"Class diagram","text":"<pre><code>classDiagram\n    class TiledMap {\n        +str path\n        +str base_dir\n        +str orientation\n        +str render_order\n        +int width\n        +int height\n        +int tile_width\n        +int tile_height\n        +bool infinite\n        +str background_color\n        +list~Tileset~ tilesets\n        +list~LayerType~ layers\n        +dict properties\n        +visible_layers list~TileLayer~\n        +get_layer(name) LayerType\n        +get_tile_layers() list~TileLayer~\n        +get_object_layers() list~ObjectLayer~\n        +get_tileset_for_gid(gid) Tileset\n        +get_tile_gid(tx, ty, layer_name) int\n        +world_to_tile(x, y, scale, offset) tuple\n        +tile_to_world(tx, ty, scale, offset) tuple\n        +draw_layer(surface, name, offset, scale)\n        +draw_all_layers(surface, offset, scale)\n        -_parse(path)\n        -_parse_tileset_ref(elem) Tileset\n        -_parse_tsx(firstgid, path) Tileset\n        -_parse_tile_layer(elem) TileLayer\n        -_parse_object_layer(elem) ObjectLayer\n    }\n\n    class Tileset {\n        +str name\n        +int firstgid\n        +int tile_width\n        +int tile_height\n        +int columns\n        +int tilecount\n        +int spacing\n        +int margin\n        +dict~int,TileData~ tile_data\n        -Image _sheet\n        -dict _pil_cache\n        -dict _pygame_cache\n        +get_tile_image(local_id) Image\n        +get_pygame_surface(local_id, flags) Surface\n        +is_empty_tile(local_id) bool\n        +get_dominant_color(local_id) tuple\n        +contains_gid(gid) bool\n        +global_to_local(gid) int\n        +clear_pygame_cache()\n        -_crop_tile(local_id) Image\n        -_build_surface(local_id, flags) Surface\n    }\n\n    class TileData {\n        +int local_id\n        +str tile_class\n        +dict properties\n        +list collision_objects\n        +list animation\n        +int width\n        +int height\n    }\n\n    class TileFlags {\n        +bool flip_h\n        +bool flip_v\n        +bool flip_d\n    }\n\n    class TileLayer {\n        +int id\n        +str name\n        +bool visible\n        +float opacity\n        +float offset_x\n        +float offset_y\n        +dict properties\n        +int min_x\n        +int min_y\n        +int max_x\n        +int max_y\n        +int width\n        +int height\n        -dict _data\n        -list~Tileset~ _tilesets\n        +load_from_flat(data, width, height)\n        +load_from_chunks(chunks)\n        +get_raw_gid(tx, ty) int\n        +get_tile(tx, ty) TileData\n        +get_tileset_by_gid(gid) Tileset\n        +iter_tiles() Iterator\n        +get_tile_by_property(prop, value) list\n    }\n\n    class ObjectLayer {\n        +int id\n        +str name\n        +bool visible\n        +float opacity\n        +str color\n        +list~TileObject~ objects\n        +dict properties\n        +get_object(name) TileObject\n        +get_objects_by_type(type) list\n    }\n\n    class TileObject {\n        +int id\n        +str name\n        +str type\n        +float x\n        +float y\n        +float width\n        +float height\n        +float rotation\n        +bool visible\n        +dict properties\n        +int gid\n    }\n\n    class OFFSET {\n        &lt;&lt;enum&gt;&gt;\n        LEFT_TOP\n        MIDDLE_TOP\n        RIGHT_TOP\n        LEFT_MIDDLE\n        CENTER\n        RIGHT_MIDDLE\n        LEFT_BOTTOM\n        MIDDLE_BOTTOM\n        RIGHT_BOTTOM\n    }\n\n    TiledMap \"1\" --&gt; \"0..*\" Tileset : tilesets\n    TiledMap \"1\" --&gt; \"0..*\" TileLayer : layers\n    TiledMap \"1\" --&gt; \"0..*\" ObjectLayer : layers\n    TiledMap ..&gt; TileLayer : injects _tilesets after parse\n    TiledMap ..&gt; OFFSET : world_to_tile / tile_to_world\n    TileLayer \"0..*\" --&gt; \"0..*\" Tileset : _tilesets\n    Tileset \"1\" --&gt; \"0..*\" TileData : tile_data\n    TileData ..&gt; TileFlags : decoded via decode_gid()\n    ObjectLayer \"1\" --&gt; \"0..*\" TileObject : objects</code></pre>"},{"location":"architecture/#renderer-module","title":"Renderer module","text":"<pre><code>classDiagram\n    class renderer {\n        &lt;&lt;module&gt;&gt;\n        -dict _surface_cache\n        -dict _scaled_cache\n        +get_cached_surface(gid, tilesets) Surface\n        +draw_layer(surface, layer, tilesets, tile_width, tile_height, offset, scale)\n        +clear_surface_cache()\n        +cache_stats() dict\n        -_find_tileset(gid, tilesets) Tileset\n        -_get_scaled_surface(surf, w, h) Surface\n    }\n\n    class tileset_module {\n        &lt;&lt;module&gt;&gt;\n        +GID_FLIP_H : int\n        +GID_FLIP_V : int\n        +GID_FLIP_D : int\n        +GID_MASK : int\n        +decode_gid(raw_gid) tuple~int,TileFlags~\n    }\n\n    renderer ..&gt; tileset_module : uses decode_gid()\n    renderer ..&gt; Tileset : calls get_pygame_surface()</code></pre>"},{"location":"architecture/#tmx-data-decoding-flow","title":"TMX data decoding flow","text":"<pre><code>flowchart LR\n    A[raw GID\\n0x80000000 bits] --&gt; B[decode_gid]\n    B --&gt; C[real_gid\\nGID_MASK applied]\n    B --&gt; D[TileFlags\\nflip_h \u00b7 flip_v \u00b7 flip_d]\n\n    C --&gt; E{Cache hit?}\n    E -- yes --&gt; F[pygame.Surface\\nfrom cache]\n    E -- no --&gt; G[_find_tileset\\nbinary search]\n    G --&gt; H[global_to_local\\ngid - firstgid]\n    H --&gt; I[Tileset._crop_tile\\nPillow crop]\n    I --&gt; J{Flags?}\n    J -- flip/rotate --&gt; K[PIL.Image.transpose]\n    J -- none --&gt; L[PIL.tobytes]\n    K --&gt; L\n    L --&gt; M[pygame.image.fromstring\\n.convert_alpha]\n    M --&gt; N[store in _surface_cache]\n    N --&gt; F</code></pre>"},{"location":"architecture/#cache-architecture","title":"Cache architecture","text":"<pre><code>flowchart TD\n    subgraph \"renderer.py \u2014 global caches\"\n        SC[\"_surface_cache\\n(firstgid, local_id, fh, fv, fd)\\n\u2192 pygame.Surface\"]\n        KC[\"_scaled_cache\\n(id surf, w, h)\\n\u2192 pygame.Surface\"]\n    end\n\n    subgraph \"Tileset._pygame_cache \u2014 per instance\"\n        TC[\"(local_id, fh, fv, fd)\\n\u2192 pygame.Surface\"]\n    end\n\n    subgraph \"Tileset._pil_cache \u2014 per instance\"\n        PC[\"local_id\\n\u2192 PIL.Image\"]\n    end\n\n    R[draw_layer call] --&gt; SC\n    SC -- miss --&gt; TC\n    TC -- miss --&gt; PC\n    PC -- miss --&gt; CROP[Pillow crop\\nfrom spritesheet]\n    CROP --&gt; PC\n\n    SC -- scale != 1 --&gt; KC</code></pre>"},{"location":"interaction/","title":"Interaction diagrams","text":""},{"location":"interaction/#loading-a-tmx-map","title":"Loading a TMX map","text":"<p>Sequence from <code>TiledMap(\"map.tmx\")</code> to the moment it's ready to render.</p> <pre><code>sequenceDiagram\n    actor User\n    participant TM as TiledMap\n    participant ET as xml.etree\n    participant TS as Tileset\n    participant PIL as Pillow (Image)\n    participant TL as TileLayer\n    participant OL as ObjectLayer\n\n    User-&gt;&gt;TM: TiledMap(\"map.tmx\")\n    activate TM\n    TM-&gt;&gt;ET: ET.parse(\"map.tmx\")\n    ET--&gt;&gt;TM: root Element\n\n    loop for each &lt;tileset&gt; in root\n        alt source=\".tsx\"\n            TM-&gt;&gt;ET: ET.parse(\"tileset.tsx\")\n            ET--&gt;&gt;TM: tsx root Element\n        end\n        TM-&gt;&gt;PIL: Image.open(image_path).convert(\"RGBA\")\n        PIL--&gt;&gt;TM: spritesheet Image\n        TM-&gt;&gt;TS: Tileset(name, firstgid, sheet, ...)\n        TS--&gt;&gt;TM: Tileset instance\n    end\n\n    loop for each layer in root\n        alt tag == \"layer\"\n            TM-&gt;&gt;TL: TileLayer(id, name, ...)\n            TM-&gt;&gt;TM: _decode_data(encoding, compression)\n            alt infinite map\n                TM-&gt;&gt;TL: load_from_chunks(chunks)\n            else finite map\n                TM-&gt;&gt;TL: load_from_flat(data, w, h)\n            end\n            TL--&gt;&gt;TM: TileLayer ready\n        else tag == \"objectgroup\"\n            TM-&gt;&gt;OL: ObjectLayer(id, name, objects, ...)\n            OL--&gt;&gt;TM: ObjectLayer ready\n        end\n    end\n\n    TM--&gt;&gt;User: TiledMap ready\n    deactivate TM</code></pre>"},{"location":"interaction/#draw_layer-one-frame","title":"draw_layer() \u2014 one frame","text":"<p>Full call chain from <code>tmap.draw_layer(screen, \"ground\")</code> down to <code>surface.blit</code>.</p> <pre><code>sequenceDiagram\n    actor User\n    participant TM as TiledMap\n    participant R as renderer\n    participant RC as _surface_cache (dict)\n    participant TS as Tileset\n    participant PIL as Pillow (Image)\n    participant PG as pygame\n\n    User-&gt;&gt;TM: draw_layer(screen, \"ground\", offset)\n    TM-&gt;&gt;R: draw_layer(surface, layer, tilesets, ...)\n\n    loop for each (tx, ty, raw_gid) in layer.iter_tiles()\n        R-&gt;&gt;R: culling check (viewport bounds)\n        alt tile outside viewport\n            R--&gt;&gt;R: skip\n        else tile inside viewport\n            R-&gt;&gt;R: decode_gid(raw_gid) \u2192 real_gid, TileFlags\n            R-&gt;&gt;RC: lookup (firstgid, local_id, fh, fv, fd)\n            alt cache hit\n                RC--&gt;&gt;R: pygame.Surface\n            else cache miss\n                R-&gt;&gt;TS: get_pygame_surface(local_id, flags)\n                TS-&gt;&gt;TS: lookup _pil_cache[local_id]\n                alt PIL cache miss\n                    TS-&gt;&gt;PIL: sheet.crop(x, y, x+tw, y+th)\n                    PIL--&gt;&gt;TS: tile Image\n                    TS-&gt;&gt;TS: store in _pil_cache\n                end\n                opt flags.flip_d or flip_h or flip_v\n                    TS-&gt;&gt;PIL: img.transpose(...)\n                    PIL--&gt;&gt;TS: flipped Image\n                end\n                TS-&gt;&gt;PIL: img.tobytes()\n                PIL--&gt;&gt;TS: raw bytes\n                TS-&gt;&gt;PG: pygame.image.fromstring(...).convert_alpha()\n                PG--&gt;&gt;TS: pygame.Surface\n                TS--&gt;&gt;R: pygame.Surface\n                R-&gt;&gt;RC: store surface\n            end\n            opt scale != 1\n                R-&gt;&gt;R: _get_scaled_surface (scaled cache)\n                R-&gt;&gt;PG: pygame.transform.scale(surf, (w,h))\n                PG--&gt;&gt;R: scaled Surface\n            end\n            R-&gt;&gt;PG: surface.blit(tile_surf, (px, py))\n        end\n    end\n\n    R--&gt;&gt;TM: done\n    TM--&gt;&gt;User: (returns None)</code></pre>"},{"location":"interaction/#tileset-sprite-detection-pillow-helpers","title":"Tileset sprite detection (Pillow helpers)","text":"<pre><code>sequenceDiagram\n    actor Dev\n    participant TS as Tileset\n    participant PIL as Pillow (Image)\n\n    Dev-&gt;&gt;TS: is_empty_tile(local_id)\n    TS-&gt;&gt;TS: get_tile_image(local_id)\n    TS-&gt;&gt;PIL: sheet.crop(x, y, w, h)\n    PIL--&gt;&gt;TS: RGBA Image\n    TS-&gt;&gt;PIL: img.split() \u2192 r,g,b,a\n    TS-&gt;&gt;PIL: a.getextrema()\n    PIL--&gt;&gt;TS: (min_alpha, max_alpha)\n    TS--&gt;&gt;Dev: max_alpha == 0  \u2192 True/False\n\n    Dev-&gt;&gt;TS: get_dominant_color(local_id)\n    TS-&gt;&gt;TS: get_tile_image(local_id)\n    TS-&gt;&gt;PIL: img.getdata()\n    PIL--&gt;&gt;TS: pixel list [(r,g,b,a), ...]\n    TS-&gt;&gt;TS: filter alpha &gt; 0, average RGB\n    TS--&gt;&gt;Dev: (r, g, b)</code></pre>"},{"location":"interaction/#cache-lifecycle","title":"Cache lifecycle","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; Empty : module imported\n\n    Empty --&gt; TileCached : first draw_layer() call\\nfor a given GID\n    TileCached --&gt; TileCached : subsequent frames\\n(cache hit, no allocation)\n\n    TileCached --&gt; ScaleCached : scale != 1\\nfirst occurrence\n    ScaleCached --&gt; ScaleCached : subsequent frames\\n(scaled cache hit)\n\n    TileCached --&gt; Empty : clear_surface_cache()\n    ScaleCached --&gt; Empty : clear_surface_cache()\n\n    note right of TileCached\n        key: (firstgid, local_id,\n              flip_h, flip_v, flip_d)\n    end note\n\n    note right of ScaleCached\n        key: (id(surf), width, height)\n    end note</code></pre>"},{"location":"api/layer/","title":"Layers","text":"<p>tiledpy has two layer types: <code>TileLayer</code> for tile data and <code>ObjectLayer</code> for vector objects.</p>"},{"location":"api/layer/#tilelayer","title":"TileLayer","text":""},{"location":"api/layer/#tiledpy.layer.TileLayer","title":"<code>tiledpy.layer.TileLayer</code>","text":"<p>A tile layer from a Tiled map (<code>&lt;layer&gt;</code> element).</p> <p>Stores tile GIDs in a sparse dict <code>{(tx, ty): raw_gid}</code> so that empty tiles (GID 0) consume no memory. Supports both finite maps (flat array) and infinite maps (chunk-based).</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>int</code> <p>Layer ID from the TMX file.</p> required <code>name</code> <code>str</code> <p>Layer name.</p> required <code>visible</code> <code>bool</code> <p>Initial visibility flag.</p> required <code>opacity</code> <code>float</code> <p>Layer opacity in the range 0.0\u20131.0.</p> required <code>offset_x</code> <code>float</code> <p>Horizontal pixel offset, by default <code>0.0</code>.</p> <code>0.0</code> <code>offset_y</code> <code>float</code> <p>Vertical pixel offset, by default <code>0.0</code>.</p> <code>0.0</code> <code>properties</code> <code>dict or None</code> <p>Custom layer properties, by default an empty dict.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>_tilesets</code> <code>list[Tileset]</code> <p>Reference to the map's tilesets, assigned by :class:<code>~tiledpy.loader.TiledMap</code> after parsing.  Used internally by :meth:<code>get_tile_by_property</code> to resolve tile metadata. Do not modify this list directly.</p> <code>min_x</code> <code>int</code> <p>Left-most tile X coordinate (computed after loading data).</p> <code>min_y</code> <code>int</code> <p>Top-most tile Y coordinate (computed after loading data).</p> <code>max_x</code> <code>int</code> <p>Right-most tile X coordinate (computed after loading data).</p> <code>max_y</code> <code>int</code> <p>Bottom-most tile Y coordinate (computed after loading data).</p> <code>width</code> <code>int</code> <p>Computed property: <code>max_x - min_x + 1</code>.</p> <code>height</code> <code>int</code> <p>Computed property: <code>max_y - min_y + 1</code>.</p>"},{"location":"api/layer/#tiledpy.layer.TileLayer.width","title":"<code>width</code>  <code>property</code>","text":"<p>int : Computed layer width in tiles (<code>max_x - min_x + 1</code>).</p>"},{"location":"api/layer/#tiledpy.layer.TileLayer.height","title":"<code>height</code>  <code>property</code>","text":"<p>int : Computed layer height in tiles (<code>max_y - min_y + 1</code>).</p>"},{"location":"api/layer/#tiledpy.layer.TileLayer.load_from_flat","title":"<code>load_from_flat(data, width, height)</code>","text":"<p>Populate the layer from a flat row-major list of raw GIDs.</p> <p>Used for finite maps where tile data is stored as a single array. Empty tiles (GID 0) are skipped.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>list[int]</code> <p>Raw GID values in row-major order (left-to-right, top-to-bottom).</p> required <code>width</code> <code>int</code> <p>Map width in tiles (number of columns).</p> required <code>height</code> <code>int</code> <p>Map height in tiles (number of rows).</p> required"},{"location":"api/layer/#tiledpy.layer.TileLayer.load_from_chunks","title":"<code>load_from_chunks(chunks)</code>","text":"<p>Populate the layer from a list of chunk dicts (infinite maps).</p> <p>Each chunk represents a fixed-size region of the infinite map. Empty tiles (GID 0) are skipped.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[dict]</code> <p>Each dict must contain the keys: <code>x</code>, <code>y</code> (chunk origin in tile coordinates), <code>width</code>, <code>height</code> (chunk dimensions in tiles), and <code>data</code> (flat list of raw GIDs).</p> required"},{"location":"api/layer/#tiledpy.layer.TileLayer.get_raw_gid","title":"<code>get_raw_gid(tx, ty)</code>","text":"<p>Return the raw GID (including flip flags) at tile coordinates.</p> <p>Parameters:</p> Name Type Description Default <code>tx</code> <code>int</code> <p>Tile X coordinate.</p> required <code>ty</code> <code>int</code> <p>Tile Y coordinate.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Raw GID value (may include flip/rotate flags in the upper bits), or <code>0</code> if the tile is empty.</p>"},{"location":"api/layer/#tiledpy.layer.TileLayer.iter_tiles","title":"<code>iter_tiles()</code>","text":"<p>Iterate over all non-empty tiles in the layer.</p> <p>Yields:</p> Type Description <code>tuple[int, int, int]</code> <p><code>(tile_x, tile_y, raw_gid)</code> for each non-empty tile. The raw GID may include flip/rotate flags \u2014 pass it to :func:<code>tiledpy.tileset.decode_gid</code> to separate the real GID from the flags.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; for tx, ty, raw_gid in layer.iter_tiles():\n...     real_gid, flags = decode_gid(raw_gid)\n</code></pre>"},{"location":"api/layer/#tiledpy.layer.TileLayer.get_tile_by_property","title":"<code>get_tile_by_property(prop, value=_MISSING)</code>","text":"<p>Return tile coordinates where the tile has a given custom property.</p> <p>Iterates over all non-empty tiles in the layer, resolves their tileset metadata via <code>self._tilesets</code> (populated by :class:<code>~tiledpy.loader.TiledMap</code> after parsing), and returns the <code>(tx, ty)</code> positions of tiles whose custom property prop matches the requested value.</p> <p>The reserved name <code>\"Class\"</code> searches against the tile's Tiled <code>class</code> attribute (<code>type</code> in older Tiled versions) stored in :attr:<code>~tiledpy.tileset.TileData.tile_class</code>, not the custom properties dict.</p> <p>Parameters:</p> Name Type Description Default <code>prop</code> <code>str</code> <p>Name of the custom property to search for (as defined in Tiled), or <code>\"Class\"</code> to match against the tile's Tiled class attribute.</p> required <code>value</code> <code>Any</code> <p>If provided, only tiles whose property prop equals this value are returned.  If omitted, every tile that has the property (regardless of its value) is returned.  When <code>prop=\"Class\"</code> and value is omitted, every tile with a non-empty class is returned.</p> <code>_MISSING</code> <p>Returns:</p> Type Description <code>list[tuple[int, int]]</code> <p>Tile coordinates <code>(tx, ty)</code> of all matching tiles. Returns an empty list if no tile matches.</p> <p>Examples:</p> <p>Find all spawn-point tiles by their Tiled class:</p> <pre><code>&gt;&gt;&gt; spawns = layer.get_tile_by_property(\"Class\", \"spawn\")\n&gt;&gt;&gt; pixel_positions = [(tx * tmap.tile_width, ty * tmap.tile_height)\n...                    for tx, ty in spawns]\n</code></pre> <p>Find all spawn-point tiles by a custom boolean property:</p> <pre><code>&gt;&gt;&gt; spawns = layer.get_tile_by_property(\"player_spawn\", True)\n</code></pre> <p>Find every tile that has a property, regardless of its value:</p> <pre><code>&gt;&gt;&gt; blocked = layer.get_tile_by_property(\"collidable\")\n</code></pre>"},{"location":"api/layer/#objectlayer","title":"ObjectLayer","text":""},{"location":"api/layer/#tiledpy.layer.ObjectLayer","title":"<code>tiledpy.layer.ObjectLayer</code>  <code>dataclass</code>","text":"<p>A Tiled object layer (<code>&lt;objectgroup&gt;</code> element).</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>int</code> <p>Layer ID from the TMX file.</p> required <code>name</code> <code>str</code> <p>Layer name.</p> required <code>visible</code> <code>bool</code> <p>Visibility flag.</p> required <code>opacity</code> <code>float</code> <p>Layer opacity in the range 0.0\u20131.0.</p> required <code>color</code> <code>str or None</code> <p>Layer color as a hex string (e.g. <code>\"#ff0000\"</code>), or <code>None</code>.</p> required <code>objects</code> <code>list[TileObject]</code> <p>Objects in this layer, by default an empty list.</p> <code>list()</code> <code>properties</code> <code>dict</code> <p>Custom layer properties, by default an empty dict.</p> <code>dict()</code>"},{"location":"api/layer/#tiledpy.layer.ObjectLayer.get_object","title":"<code>get_object(name)</code>","text":"<p>Return the first object whose name matches.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Object name to search for.</p> required <p>Returns:</p> Type Description <code>TileObject or None</code> <p>The matching object, or <code>None</code> if not found.</p>"},{"location":"api/layer/#tiledpy.layer.ObjectLayer.get_objects_by_type","title":"<code>get_objects_by_type(type_)</code>","text":"<p>Return all objects whose type matches.</p> <p>Parameters:</p> Name Type Description Default <code>type_</code> <code>str</code> <p>Object type or class to filter by.</p> required <p>Returns:</p> Type Description <code>list[TileObject]</code> <p>List of matching objects (may be empty).</p>"},{"location":"api/layer/#tileobject","title":"TileObject","text":""},{"location":"api/layer/#tiledpy.layer.TileObject","title":"<code>tiledpy.layer.TileObject</code>  <code>dataclass</code>","text":"<p>A single object within an ObjectLayer.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>int</code> <p>Unique object ID assigned by Tiled.</p> required <code>name</code> <code>str</code> <p>Object name.</p> required <code>type</code> <code>str</code> <p>Object type or class (<code>class</code> attribute in Tiled 1.9+).</p> required <code>x</code> <code>float</code> <p>X position in pixels (top-left corner).</p> required <code>y</code> <code>float</code> <p>Y position in pixels (top-left corner).</p> required <code>width</code> <code>float</code> <p>Width in pixels.</p> required <code>height</code> <code>float</code> <p>Height in pixels.</p> required <code>rotation</code> <code>float</code> <p>Clockwise rotation in degrees, by default <code>0.0</code>.</p> <code>0.0</code> <code>visible</code> <code>bool</code> <p>Whether the object is visible, by default <code>True</code>.</p> <code>True</code> <code>properties</code> <code>dict</code> <p>Custom properties defined in Tiled, by default an empty dict.</p> <code>dict()</code> <code>gid</code> <code>int or None</code> <p>Global tile ID if this is a tile-object, otherwise <code>None</code>.</p> <code>None</code>"},{"location":"api/renderer/","title":"Renderer","text":"<p><code>tiledpy.renderer</code> is a module-level rendering system for Pygame. It maintains two global caches that persist across frames and map reloads:</p> Cache Key Value <code>_surface_cache</code> <code>(firstgid, local_id, flip_h, flip_v, flip_d)</code> <code>pygame.Surface</code> <code>_scaled_cache</code> <code>(id(surf), width, height)</code> <code>pygame.Surface</code> (scaled) <p>You generally don't import this module directly \u2014 <code>TiledMap.draw_layer()</code> calls it internally.</p>"},{"location":"api/renderer/#draw_layer","title":"draw_layer","text":""},{"location":"api/renderer/#tiledpy.renderer.draw_layer","title":"<code>tiledpy.renderer.draw_layer(surface, layer, tilesets, tile_width, tile_height, offset=(0, 0), scale=1)</code>","text":"<p>Render a TileLayer onto a <code>pygame.Surface</code>.</p> <p>Iterates non-empty tiles, applies viewport culling, fetches cached surfaces, and blits each tile. If <code>layer.opacity &lt; 1.0</code> a surface copy with <code>set_alpha()</code> is used \u2014 one allocation per blit, so keep opacity at <code>1.0</code> for performance-critical layers.</p> <p>Parameters:</p> Name Type Description Default <code>surface</code> <code>Surface</code> <p>Render target.</p> required <code>layer</code> <code>TileLayer</code> <p>The tile layer to draw.</p> required <code>tilesets</code> <code>list[Tileset]</code> <p>All tilesets of the map, sorted by <code>firstgid</code> ascending.</p> required <code>tile_width</code> <code>int</code> <p>Base tile width in pixels (from the map).</p> required <code>tile_height</code> <code>int</code> <p>Base tile height in pixels (from the map).</p> required <code>offset</code> <code>tuple[int, int]</code> <p>Camera offset <code>(ox, oy)</code> in pixels, by default <code>(0, 0)</code>.</p> <code>(0, 0)</code> <code>scale</code> <code>int</code> <p>Integer scale factor for pixel-art rendering, by default <code>1</code>.</p> <code>1</code>"},{"location":"api/renderer/#get_cached_surface","title":"get_cached_surface","text":""},{"location":"api/renderer/#tiledpy.renderer.get_cached_surface","title":"<code>tiledpy.renderer.get_cached_surface(gid, tilesets)</code>","text":"<p>Return the <code>pygame.Surface</code> for the given raw global tile ID.</p> <p>Decodes flip flags from the raw GID, locates the owning tileset, and returns a cached surface. Creates and caches the surface on the first call for each unique <code>(tileset, local_id, flags)</code> combination.</p> <p>Parameters:</p> Name Type Description Default <code>gid</code> <code>int</code> <p>Raw GID value as stored in a TileLayer (may include flip flags).</p> required <code>tilesets</code> <code>list[Tileset]</code> <p>All tilesets for the map, sorted by <code>firstgid</code> ascending.</p> required <p>Returns:</p> Type Description <code>Surface or None</code> <p>The tile surface with <code>convert_alpha</code> applied, or <code>None</code> if the GID is 0 (empty tile) or not found in any tileset.</p>"},{"location":"api/renderer/#clear_surface_cache","title":"clear_surface_cache","text":""},{"location":"api/renderer/#tiledpy.renderer.clear_surface_cache","title":"<code>tiledpy.renderer.clear_surface_cache()</code>","text":"<p>Clear both global pygame surface caches.</p> <p>Call this when:</p> <ul> <li>Loading a completely different map.</li> <li>Changing the scale factor at runtime.</li> <li>Freeing memory.</li> </ul> Notes <p>The next call to :func:<code>draw_layer</code> will rebuild both caches from scratch, which may cause a brief stutter on large maps.</p>"},{"location":"api/renderer/#cache_stats","title":"cache_stats","text":""},{"location":"api/renderer/#tiledpy.renderer.cache_stats","title":"<code>tiledpy.renderer.cache_stats()</code>","text":"<p>Return current entry counts for both surface caches.</p> <p>Returns:</p> Type Description <code>dict</code> <p>Dictionary with keys <code>\"tile_surfaces\"</code> and <code>\"scaled_surfaces\"</code>, each mapping to an integer count.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; if frame % 300 == 0:\n...     print(cache_stats())\n{'tile_surfaces': 47, 'scaled_surfaces': 0}\n</code></pre>"},{"location":"api/renderer/#performance-tips","title":"Performance tips","text":"<pre><code>flowchart LR\n    A[First frame] --&gt; B[Cache miss\\nPillow crop + pygame convert]\n    B --&gt; C[Stored in\\n_surface_cache]\n    C --&gt; D[Subsequent frames]\n    D --&gt; E[Cache hit\\ndirect dict lookup O1]\n    E --&gt; F[surface.blit]</code></pre> <ul> <li>Keep <code>scale</code> constant across frames \u2014 changing it invalidates <code>_scaled_cache</code></li> <li>Use <code>layer.visible = False</code> instead of conditional <code>draw_layer()</code> calls; the   culling loop still runs but nothing is blitted</li> <li>For very large maps, the number of cache entries equals the number of   unique <code>(tileset, local_id, flip flags)</code> combinations used \u2014 not the   total tile count</li> </ul>"},{"location":"api/tiledmap/","title":"TiledMap","text":"<p><code>tiledpy.TiledMap</code> is the main entry point. It parses a <code>.tmx</code> file and exposes layers, tilesets, and rendering methods.</p> <pre><code>from tiledpy import TiledMap\n\ntmap = TiledMap(\"map.tmx\")\n</code></pre>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap","title":"<code>tiledpy.loader.TiledMap</code>","text":"<p>A Tiled map loaded from a <code>.tmx</code> file.</p> <p>Parses the TMX XML, builds :class:<code>~tiledpy.tileset.Tileset</code>, :class:<code>~tiledpy.layer.TileLayer</code>, and :class:<code>~tiledpy.layer.ObjectLayer</code> objects, and exposes rendering helpers for Pygame.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Absolute or relative path to the <code>.tmx</code> file.</p> required <p>Attributes:</p> Name Type Description <code>path</code> <code>str</code> <p>Absolute path to the <code>.tmx</code> file.</p> <code>base_dir</code> <code>str</code> <p>Directory containing the <code>.tmx</code> (used to resolve relative paths to images and external tilesets).</p> <code>orientation</code> <code>str</code> <p>Map orientation: <code>\"orthogonal\"</code>, <code>\"isometric\"</code>, etc.</p> <code>render_order</code> <code>str</code> <p>Tile render order, e.g. <code>\"right-down\"</code> (default).</p> <code>width</code> <code>int</code> <p>Map width in tiles.</p> <code>height</code> <code>int</code> <p>Map height in tiles.</p> <code>tile_width</code> <code>int</code> <p>Tile width in pixels.</p> <code>tile_height</code> <code>int</code> <p>Tile height in pixels.</p> <code>infinite</code> <code>bool</code> <p><code>True</code> if the map uses chunk-based infinite scrolling.</p> <code>background_color</code> <code>str or None</code> <p>Hex background color string <code>\"#rrggbb\"</code>, or <code>None</code>.</p> <code>tilesets</code> <code>list[Tileset]</code> <p>All tilesets sorted by <code>firstgid</code> ascending.</p> <code>layers</code> <code>list[TileLayer or ObjectLayer]</code> <p>All layers in draw order.</p> <code>properties</code> <code>dict</code> <p>Custom map-level properties.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If any tileset image referenced by the TMX/TSX is missing.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; tmap = TiledMap(\"map.tmx\")\n&gt;&gt;&gt; tmap.draw_layer(screen, \"ground\", offset=(cam_x, cam_y))\n</code></pre>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.get_layer","title":"<code>get_layer(name)</code>","text":"<p>Return the first layer whose name matches.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Layer name to look up.</p> required <p>Returns:</p> Type Description <code>TileLayer or ObjectLayer or None</code> <p>The matching layer, or <code>None</code> if not found.</p>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.get_tile_layers","title":"<code>get_tile_layers()</code>","text":"<p>Return all TileLayer instances in draw order.</p> <p>Returns:</p> Type Description <code>list[TileLayer]</code> <p>All tile layers from the map.</p>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.get_object_layers","title":"<code>get_object_layers()</code>","text":"<p>Return all ObjectLayer instances in draw order.</p> <p>Returns:</p> Type Description <code>list[ObjectLayer]</code> <p>All object layers from the map.</p>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.world_to_tile","title":"<code>world_to_tile(x, y, scale=1, offset=OFFSET.LEFT_TOP)</code>","text":"<p>Convert world pixel coordinates to tile coordinates.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>float</code> <p>World X position in pixels.</p> required <code>y</code> <code>float</code> <p>World Y position in pixels.</p> required <code>scale</code> <code>int</code> <p>Integer scale factor applied during rendering, by default <code>1</code>.</p> <code>1</code> <p>Returns:</p> Type Description <code>tuple[int, int]</code> <p><code>(tx, ty)</code> tile coordinates (integer, floor division).</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; tx, ty = tmap.world_to_tile(mouse_x + cam_x, mouse_y + cam_y, scale=2)\n</code></pre>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.tile_to_world","title":"<code>tile_to_world(tx, ty, scale=1, offset=OFFSET.LEFT_TOP)</code>","text":"<p>Convert tile coordinates to world pixel coordinates.</p> <p>Returns the top-left pixel of the tile.</p> <p>Parameters:</p> Name Type Description Default <code>tx</code> <code>int</code> <p>Tile X coordinate.</p> required <code>ty</code> <code>int</code> <p>Tile Y coordinate.</p> required <code>scale</code> <code>int</code> <p>Integer scale factor applied during rendering, by default <code>1</code>.</p> <code>1</code> <p>Returns:</p> Type Description <code>tuple[int, int]</code> <p><code>(x, y)</code> world position in pixels (top-left corner of the tile).</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; x, y = tmap.tile_to_world(tx, ty, scale=2)\n&gt;&gt;&gt; screen.blit(marker, (x - cam_x, y - cam_y))\n</code></pre>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.get_tile_gid","title":"<code>get_tile_gid(tx, ty, layer_name=None)</code>","text":"<p>Return the raw GID at tile coordinates <code>(tx, ty)</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tx</code> <code>int</code> <p>Tile X coordinate.</p> required <code>ty</code> <code>int</code> <p>Tile Y coordinate.</p> required <code>layer_name</code> <code>str or None</code> <p>Name of the layer to query. If <code>None</code>, all :class:<code>~tiledpy.layer.TileLayer</code> instances are searched in draw order and the first non-zero GID is returned.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>Raw GID (may include flip/rotate flags). Returns <code>0</code> if the tile is empty or the layer does not exist.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; gid = tmap.get_tile_gid(3, 5, \"ground\")\n&gt;&gt;&gt; gid = tmap.get_tile_gid(3, 5)          # first non-empty layer\n</code></pre>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.get_tileset_for_gid","title":"<code>get_tileset_for_gid(gid)</code>","text":"<p>Return the Tileset that owns the given global tile ID.</p> <p>Uses a linear scan (tilesets are sorted by <code>firstgid</code>), returning the last tileset whose <code>firstgid</code> is less than or equal to <code>gid</code>.</p> <p>Parameters:</p> Name Type Description Default <code>gid</code> <code>int</code> <p>Global tile ID (without flip flags).</p> required <p>Returns:</p> Type Description <code>Tileset or None</code> <p>The owning tileset, or <code>None</code> if no tileset covers this GID.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ts = tmap.get_tileset_for_gid(42)\n&gt;&gt;&gt; local = ts.global_to_local(42)\n</code></pre>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.draw_layer","title":"<code>draw_layer(surface, layer_name, offset=(0, 0), scale=1)</code>","text":"<p>Render one named TileLayer onto a <code>pygame.Surface</code>.</p> <p>Delegates to :func:<code>tiledpy.renderer.draw_layer</code>. Tiles outside the surface bounds are skipped (viewport culling). Does nothing if the layer does not exist or is not a :class:<code>TileLayer</code>.</p> <p>Parameters:</p> Name Type Description Default <code>surface</code> <code>Surface</code> <p>Render target.</p> required <code>layer_name</code> <code>str</code> <p>Name of the layer to draw.</p> required <code>offset</code> <code>tuple[int, int]</code> <p>Camera offset <code>(ox, oy)</code> in pixels, by default <code>(0, 0)</code>.</p> <code>(0, 0)</code> <code>scale</code> <code>int</code> <p>Integer scale factor for pixel-art rendering, by default <code>1</code>.</p> <code>1</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; tmap.draw_layer(screen, \"water\", offset=(cam_x, cam_y), scale=2)\n</code></pre>"},{"location":"api/tiledmap/#tiledpy.loader.TiledMap.draw_all_layers","title":"<code>draw_all_layers(surface, offset=(0, 0), scale=1)</code>","text":"<p>Draw all visible TileLayer instances in order.</p> <p>Parameters:</p> Name Type Description Default <code>surface</code> <code>Surface</code> <p>Render target.</p> required <code>offset</code> <code>tuple[int, int]</code> <p>Camera offset <code>(ox, oy)</code> in pixels, by default <code>(0, 0)</code>.</p> <code>(0, 0)</code> <code>scale</code> <code>int</code> <p>Integer scale factor for pixel-art rendering, by default <code>1</code>.</p> <code>1</code>"},{"location":"api/tiledmap/#supported-tmx-features","title":"Supported TMX features","text":"Feature Supported Finite maps Yes Infinite maps (chunks) Yes CSV encoding Yes Base64 + zlib Yes Base64 + gzip Yes Base64 + zstd Yes (<code>pip install zstandard</code>) External <code>.tsx</code> tilesets Yes Inline tilesets Yes Tile properties Yes Tile collision objects Yes Tile animations Yes (data stored, no auto-play) Object layers Yes Flip / rotate flags Yes Group layers No Image layers No"},{"location":"api/tileset/","title":"Tileset","text":"<p><code>tiledpy.Tileset</code> loads a spritesheet image with Pillow, crops individual tile sprites, and converts them to <code>pygame.Surface</code> objects with a two-level cache.</p> <p>You rarely need to construct a <code>Tileset</code> manually \u2014 <code>TiledMap</code> builds them automatically from the <code>.tmx</code> / <code>.tsx</code> file.</p>"},{"location":"api/tileset/#tileset_1","title":"Tileset","text":""},{"location":"api/tileset/#tiledpy.tileset.Tileset","title":"<code>tiledpy.tileset.Tileset</code>","text":"<p>A tileset backed by a spritesheet image.</p> <p>Uses Pillow to crop individual tile images from the sheet and converts them to <code>pygame.Surface</code> objects on demand. Results are cached at both the Pillow level (raw crop) and the pygame level (surface per flip/rotation flags).</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Tileset name as declared in the TMX/TSX.</p> required <code>firstgid</code> <code>int</code> <p>First global tile ID assigned to this tileset.</p> required <code>image_path</code> <code>str</code> <p>Absolute path to the spritesheet image file.</p> required <code>tile_width</code> <code>int</code> <p>Width of each tile in pixels.</p> required <code>tile_height</code> <code>int</code> <p>Height of each tile in pixels.</p> required <code>columns</code> <code>int</code> <p>Number of tile columns in the spritesheet.</p> required <code>tilecount</code> <code>int</code> <p>Total number of tiles in the tileset.</p> required <code>spacing</code> <code>int</code> <p>Pixels between adjacent tiles, by default <code>0</code>.</p> <code>0</code> <code>margin</code> <code>int</code> <p>Pixels around the outer edge of the sheet, by default <code>0</code>.</p> <code>0</code> <code>tile_data</code> <code>dict[int, TileData] or None</code> <p>Per-tile metadata keyed by local ID, by default <code>None</code>.</p> <code>None</code>"},{"location":"api/tileset/#tiledpy.tileset.Tileset.get_tile_image","title":"<code>get_tile_image(local_id)</code>","text":"<p>Return the RGBA PIL crop of the tile at <code>local_id</code>.</p> <p>The result is cached in an internal PIL cache \u2014 subsequent calls for the same <code>local_id</code> are free.</p> <p>Parameters:</p> Name Type Description Default <code>local_id</code> <code>int</code> <p>Zero-based local tile index within the tileset.</p> required <p>Returns:</p> Type Description <code>Image</code> <p>RGBA image of the tile.</p>"},{"location":"api/tileset/#tiledpy.tileset.Tileset.is_empty_tile","title":"<code>is_empty_tile(local_id)</code>","text":"<p>Return <code>True</code> if the tile is fully transparent.</p> <p>Checks whether the maximum alpha value across all pixels is 0.</p> <p>Parameters:</p> Name Type Description Default <code>local_id</code> <code>int</code> <p>Zero-based local tile index.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if every pixel has alpha = 0.</p>"},{"location":"api/tileset/#tiledpy.tileset.Tileset.get_dominant_color","title":"<code>get_dominant_color(local_id)</code>","text":"<p>Return the average RGB color of all non-transparent pixels.</p> <p>Parameters:</p> Name Type Description Default <code>local_id</code> <code>int</code> <p>Zero-based local tile index.</p> required <p>Returns:</p> Type Description <code>tuple[int, int, int]</code> <p><code>(r, g, b)</code> average color. Returns <code>(0, 0, 0)</code> if the tile has no non-transparent pixels.</p>"},{"location":"api/tileset/#tiledpy.tileset.Tileset.get_pygame_surface","title":"<code>get_pygame_surface(local_id, flags=None)</code>","text":"<p>Return a pygame.Surface for the given tile with flip/rotation applied.</p> <p>The surface is cached by <code>(local_id, flip_h, flip_v, flip_d)</code> \u2014 subsequent calls for the same combination are instant dict lookups.</p> <p>Parameters:</p> Name Type Description Default <code>local_id</code> <code>int</code> <p>Zero-based local tile index.</p> required <code>flags</code> <code>TileFlags or None</code> <p>Flip/rotation flags to apply, by default no transforms.</p> <code>None</code> <p>Returns:</p> Type Description <code>Surface</code> <p>Tile surface with <code>convert_alpha()</code> applied and the requested transformations.</p>"},{"location":"api/tileset/#tiledpy.tileset.Tileset.clear_pygame_cache","title":"<code>clear_pygame_cache()</code>","text":"<p>Clear the per-instance pygame surface cache.</p> <p>Call this after resizing the window or changing the scale factor to force surfaces to be rebuilt at the new resolution.</p>"},{"location":"api/tileset/#tiledpy.tileset.Tileset.contains_gid","title":"<code>contains_gid(gid)</code>","text":"<p>Return <code>True</code> if the global GID belongs to this tileset.</p> <p>Parameters:</p> Name Type Description Default <code>gid</code> <code>int</code> <p>Global tile ID to test.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>firstgid &lt;= gid &lt; firstgid + tilecount</code>.</p>"},{"location":"api/tileset/#tiledpy.tileset.Tileset.global_to_local","title":"<code>global_to_local(gid)</code>","text":"<p>Convert a global GID to a local (0-based) tile ID.</p> <p>Parameters:</p> Name Type Description Default <code>gid</code> <code>int</code> <p>Global tile ID.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Local tile index: <code>gid - firstgid</code>.</p>"},{"location":"api/tileset/#tileflags","title":"TileFlags","text":""},{"location":"api/tileset/#tiledpy.tileset.TileFlags","title":"<code>tiledpy.tileset.TileFlags</code>  <code>dataclass</code>","text":"<p>Flip and rotation flags decoded from a raw Tiled GID.</p> <p>Attributes:</p> Name Type Description <code>flip_h</code> <code>bool</code> <p>Horizontal flip flag (GID bit <code>0x80000000</code>).</p> <code>flip_v</code> <code>bool</code> <p>Vertical flip flag (GID bit <code>0x40000000</code>).</p> <code>flip_d</code> <code>bool</code> <p>Diagonal flip flag (GID bit <code>0x20000000</code>); used for 90\u00b0 rotation when combined with <code>flip_h</code> or <code>flip_v</code>.</p>"},{"location":"api/tileset/#decode_gid","title":"decode_gid","text":""},{"location":"api/tileset/#tiledpy.tileset.decode_gid","title":"<code>tiledpy.tileset.decode_gid(raw_gid)</code>","text":"<p>Separate the real GID from Tiled's flip/rotation flags.</p> <p>Tiled encodes flip and rotation by setting the three highest bits of the 32-bit GID. This function extracts those flags and returns the clean GID.</p> <p>Parameters:</p> Name Type Description Default <code>raw_gid</code> <code>int</code> <p>Raw 32-bit GID as stored in a TileLayer (may include flag bits).</p> required <p>Returns:</p> Type Description <code>tuple[int, TileFlags]</code> <p><code>(real_gid, flags)</code> where <code>real_gid</code> has the flag bits cleared and <code>flags</code> is a :class:<code>TileFlags</code> instance.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; real_gid, flags = decode_gid(0x80000005)\n&gt;&gt;&gt; real_gid\n5\n&gt;&gt;&gt; flags.flip_h\nTrue\n</code></pre>"},{"location":"api/tileset/#tiledata","title":"TileData","text":""},{"location":"api/tileset/#tiledpy.tileset.TileData","title":"<code>tiledpy.tileset.TileData</code>  <code>dataclass</code>","text":"<p>Per-tile metadata parsed from a TSX <code>&lt;tile&gt;</code> element.</p> <p>Attributes:</p> Name Type Description <code>local_id</code> <code>int</code> <p>Zero-based local tile ID within the tileset.</p> <code>tile_class</code> <code>str</code> <p>The Tiled <code>class</code> attribute of the tile (also known as <code>type</code> in older Tiled versions).  Empty string if not set.</p> <code>properties</code> <code>dict</code> <p>Custom properties defined in Tiled for this tile.</p> <code>collision_objects</code> <code>list[dict]</code> <p>Collision rectangles, each with keys <code>x</code>, <code>y</code>, <code>width</code>, and <code>height</code> (in pixels).</p> <code>animation</code> <code>list[dict]</code> <p>Animation frames, each with keys <code>tileid</code> (int) and <code>duration</code> (int, milliseconds).</p> <code>width</code> <code>int or None</code> <p>Tile width in pixels, or <code>None</code> if using the tileset default.</p> <code>height</code> <code>int or None</code> <p>Tile height in pixels, or <code>None</code> if using the tileset default.</p>"},{"location":"guide/pygame_example/","title":"Complete Pygame example","text":"<p>This guide walks through a fully working game loop that loads a TMX map, scrolls the camera with the arrow keys, and renders every layer in order.</p>"},{"location":"guide/pygame_example/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install tiledpy pygame pillow\n</code></pre> <p>Your project directory should look like:</p> <pre><code>game/\n\u251c\u2500\u2500 main.py\n\u251c\u2500\u2500 map.tmx\n\u251c\u2500\u2500 tileset.tsx          (optional external tileset)\n\u2514\u2500\u2500 sprites/\n    \u2514\u2500\u2500 tileset.png\n</code></pre>"},{"location":"guide/pygame_example/#full-example","title":"Full example","text":"<pre><code>\"\"\"\nmain.py \u2014 Complete tiledpy + pygame example.\n\nControls:\n    Arrow keys  Move camera\n    +/-         Zoom in/out (integer scale)\n    D           Toggle debug overlay\n    ESC / Q     Quit\n\"\"\"\n\nimport sys\nimport pygame\nfrom tiledpy import TiledMap\nfrom tiledpy.renderer import cache_stats, clear_surface_cache\n\n\n# ---------------------------------------------------------------------------\n# Camera\n# ---------------------------------------------------------------------------\n\nclass Camera:\n    \"\"\"Tracks a pixel offset with clamped scrolling.\"\"\"\n\n    def __init__(self, map_pixel_w: int, map_pixel_h: int,\n                 screen_w: int, screen_h: int) -&gt; None:\n        self.x = 0\n        self.y = 0\n        self.map_w = map_pixel_w\n        self.map_h = map_pixel_h\n        self.screen_w = screen_w\n        self.screen_h = screen_h\n\n    def move(self, dx: int, dy: int) -&gt; None:\n        self.x = max(0, min(self.x + dx, self.map_w - self.screen_w))\n        self.y = max(0, min(self.y + dy, self.map_h - self.screen_h))\n\n    @property\n    def offset(self) -&gt; tuple[int, int]:\n        return self.x, self.y\n\n\n# ---------------------------------------------------------------------------\n# Debug overlay\n# ---------------------------------------------------------------------------\n\ndef draw_debug(screen: pygame.Surface, tmap: TiledMap,\n               camera: Camera, clock: pygame.time.Clock,\n               scale: int, font: pygame.font.Font) -&gt; None:\n    stats = cache_stats()\n    lines = [\n        f\"FPS: {clock.get_fps():.0f}\",\n        f\"Camera: ({camera.x}, {camera.y})\",\n        f\"Scale: {scale}x\",\n        f\"Map: {tmap.width}x{tmap.height} tiles\",\n        f\"Tile size: {tmap.tile_width}x{tmap.tile_height}px\",\n        f\"Layers: {len(tmap.layers)}\",\n        f\"Tilesets: {len(tmap.tilesets)}\",\n        f\"Surface cache: {stats['tile_surfaces']} entries\",\n        f\"Scaled cache:  {stats['scaled_surfaces']} entries\",\n    ]\n    for i, line in enumerate(lines):\n        shadow = font.render(line, True, (0, 0, 0))\n        text   = font.render(line, True, (255, 255, 200))\n        screen.blit(shadow, (11, 11 + i * 18))\n        screen.blit(text,   (10, 10 + i * 18))\n\n\n# ---------------------------------------------------------------------------\n# Main\n# ---------------------------------------------------------------------------\n\ndef main(tmx_path: str = \"map.tmx\") -&gt; None:\n    pygame.init()\n    pygame.display.set_caption(\"tiledpy demo\")\n\n    SCREEN_W, SCREEN_H = 800, 600\n    SCROLL_SPEED = 4        # pixels per frame (before scale)\n    INITIAL_SCALE = 2\n\n    screen = pygame.display.set_mode((SCREEN_W, SCREEN_H))\n    clock  = pygame.time.Clock()\n    font   = pygame.font.SysFont(\"monospace\", 14)\n\n    # ---- Load map ----------------------------------------------------------\n    tmap  = TiledMap(tmx_path)\n    scale = INITIAL_SCALE\n\n    map_pixel_w = tmap.width  * tmap.tile_width  * scale\n    map_pixel_h = tmap.height * tmap.tile_height * scale\n    camera = Camera(map_pixel_w, map_pixel_h, SCREEN_W, SCREEN_H)\n\n    # ---- Resolve background color ------------------------------------------\n    if tmap.background_color:\n        hex_color = tmap.background_color.lstrip(\"#\")\n        bg_color  = tuple(int(hex_color[i:i+2], 16) for i in (0, 2, 4))\n    else:\n        bg_color = (30, 30, 30)\n\n    # ---- Print layer info --------------------------------------------------\n    for layer in tmap.layers:\n        print(f\"  layer: {layer.name!r}  visible={layer.visible}\")\n\n    show_debug = False\n\n    # ---- Game loop ---------------------------------------------------------\n    running = True\n    while running:\n\n        # -- Events ----------------------------------------------------------\n        for event in pygame.event.get():\n            if event.type == pygame.QUIT:\n                running = False\n            if event.type == pygame.KEYDOWN:\n                if event.key in (pygame.K_ESCAPE, pygame.K_q):\n                    running = False\n                elif event.key == pygame.K_d:\n                    show_debug = not show_debug\n                elif event.key == pygame.K_PLUS:\n                    new_scale = min(scale + 1, 6)\n                    if new_scale != scale:\n                        scale = new_scale\n                        clear_surface_cache()\n                        map_pixel_w = tmap.width  * tmap.tile_width  * scale\n                        map_pixel_h = tmap.height * tmap.tile_height * scale\n                        camera = Camera(map_pixel_w, map_pixel_h,\n                                        SCREEN_W, SCREEN_H)\n                elif event.key == pygame.K_MINUS:\n                    new_scale = max(scale - 1, 1)\n                    if new_scale != scale:\n                        scale = new_scale\n                        clear_surface_cache()\n                        map_pixel_w = tmap.width  * tmap.tile_width  * scale\n                        map_pixel_h = tmap.height * tmap.tile_height * scale\n                        camera = Camera(map_pixel_w, map_pixel_h,\n                                        SCREEN_W, SCREEN_H)\n\n        # -- Camera scroll ---------------------------------------------------\n        keys = pygame.key.get_pressed()\n        dx = dy = 0\n        if keys[pygame.K_LEFT]  or keys[pygame.K_a]: dx -= SCROLL_SPEED * scale\n        if keys[pygame.K_RIGHT] or keys[pygame.K_d]: dx += SCROLL_SPEED * scale\n        if keys[pygame.K_UP]    or keys[pygame.K_w]: dy -= SCROLL_SPEED * scale\n        if keys[pygame.K_DOWN]  or keys[pygame.K_s]: dy += SCROLL_SPEED * scale\n        camera.move(dx, dy)\n\n        # -- Draw ------------------------------------------------------------\n        screen.fill(bg_color)\n\n        # Draw every visible tile layer in order\n        tmap.draw_all_layers(screen, offset=camera.offset, scale=scale)\n\n        if show_debug:\n            draw_debug(screen, tmap, camera, clock, scale, font)\n\n        pygame.display.flip()\n        clock.tick(60)\n\n    pygame.quit()\n    sys.exit(0)\n\n\nif __name__ == \"__main__\":\n    path = sys.argv[1] if len(sys.argv) &gt; 1 else \"map.tmx\"\n    main(path)\n</code></pre>"},{"location":"guide/pygame_example/#running-it","title":"Running it","text":"<pre><code>python main.py map.tmx\n</code></pre> <p>Or pass any TMX path:</p> <pre><code>python main.py \"../TileMap/Map.tmx\"\n</code></pre>"},{"location":"guide/pygame_example/#controls-reference","title":"Controls reference","text":"Key Action Arrow keys / WASD Scroll camera <code>+</code> / <code>-</code> Zoom in / out (integer scale 1\u20136) <code>D</code> Toggle debug overlay <code>ESC</code> / <code>Q</code> Quit"},{"location":"guide/pygame_example/#drawing-individual-layers","title":"Drawing individual layers","text":"<p>You can control which layers to draw and in what order:</p> <pre><code># Draw only the ground layer below the player\ntmap.draw_layer(screen, \"ground\", offset=camera.offset, scale=scale)\n\n# --- draw player sprite here ---\nscreen.blit(player_surf, player_screen_pos)\n\n# Draw the \"above\" layer on top of the player\ntmap.draw_layer(screen, \"above\", offset=camera.offset, scale=scale)\n</code></pre>"},{"location":"guide/pygame_example/#querying-layers","title":"Querying layers","text":"<pre><code># All tile layers in draw order\ntile_layers = tmap.get_tile_layers()\n\n# All object layers in draw order\nobject_layers = tmap.get_object_layers()\n\n# Only visible tile layers (shorthand property)\nfor layer in tmap.visible_layers:\n    print(layer.name)\n</code></pre> <p>Use <code>get_tile_gid()</code> to read the raw GID at a position without going through a layer reference directly:</p> <pre><code># From a specific layer\nraw = tmap.get_tile_gid(tx, ty, \"ground\")\n\n# From the first non-empty layer at those coords\nraw = tmap.get_tile_gid(tx, ty)\n</code></pre>"},{"location":"guide/pygame_example/#reading-object-layers","title":"Reading object layers","text":"<pre><code>entities_layer = tmap.get_layer(\"entities\")\n\n# Place the player at the spawn point\nspawn = entities_layer.get_object(\"player_spawn\")\nif spawn:\n    player_world_x = spawn.x\n    player_world_y = spawn.y\n\n# Iterate all enemies\nfor obj in entities_layer.get_objects_by_type(\"enemy\"):\n    enemy_list.append(Enemy(obj.x, obj.y, obj.properties))\n</code></pre>"},{"location":"guide/pygame_example/#coordinate-conversion","title":"Coordinate conversion","text":"<p><code>TiledMap</code> provides built-in helpers to convert between world pixels and tile coordinates. Both accept an optional <code>scale</code> (integer render scale) and an <code>OFFSET</code> enum value that shifts the anchor point used for the conversion.</p> <pre><code>from tiledpy.enums import OFFSET\n\n# World pixel \u2192 tile (default anchor: top-left corner of the tile)\ntx, ty = tmap.world_to_tile(mouse_x + cam_x, mouse_y + cam_y, scale=2)\n\n# Tile \u2192 world pixel (returns the top-left corner of the tile)\nwx, wy = tmap.tile_to_world(tx, ty, scale=2)\nscreen.blit(highlight_surf, (wx - cam_x, wy - cam_y))\n\n# Use OFFSET.CENTER to snap to tile centers instead of top-left corners\ncx, cy = tmap.tile_to_world(tx, ty, scale=2, offset=OFFSET.CENTER)\n</code></pre> <p>Available <code>OFFSET</code> values: <code>LEFT_TOP</code>, <code>MIDDLE_TOP</code>, <code>RIGHT_TOP</code>, <code>LEFT_MIDDLE</code>, <code>CENTER</code>, <code>RIGHT_MIDDLE</code>, <code>LEFT_BOTTOM</code>, <code>MIDDLE_BOTTOM</code>, <code>RIGHT_BOTTOM</code>.</p>"},{"location":"guide/pygame_example/#collision-detection-using-tile-properties","title":"Collision detection using tile properties","text":"<p><code>TileLayer.get_tile(tx, ty)</code> resolves the full <code>TileData</code> for a tile in one call \u2014 no need to manually decode the GID, look up the tileset, and index <code>tile_data</code>:</p> <pre><code>collision_layer = tmap.get_layer(\"collision\")\n\ndef tile_is_solid(tx: int, ty: int) -&gt; bool:\n    td = collision_layer.get_tile(tx, ty)\n    if td is None:\n        return False\n    return bool(td.properties.get(\"collision\", False))\n\n\n# Example: check if player's feet are on a solid tile\nfoot_x, foot_y = player_rect.midbottom\ntx, ty = tmap.world_to_tile(foot_x, foot_y)   # uses built-in helper\nif tile_is_solid(tx, ty):\n    player.on_ground = True\n</code></pre> <p>You can also look up tiles by a Tiled class or any custom property directly on the layer:</p> <pre><code># All tiles whose Tiled class is \"spike\"\nspike_coords = collision_layer.get_tile_by_property(\"Class\", \"spike\")\n\n# All tiles where the custom bool property \"lethal\" is True\nlethal_coords = collision_layer.get_tile_by_property(\"lethal\", True)\n\n# All tiles that have a \"damage\" property (any value)\ndamaging = collision_layer.get_tile_by_property(\"damage\")\n</code></pre>"},{"location":"guide/pygame_example/#inspect-sprite-data-with-pillow","title":"Inspect sprite data with Pillow","text":"<pre><code>from tiledpy.tileset import decode_gid\n\nlayer = tmap.get_layer(\"decorations\")\n\n# Get dominant color of every unique tile\nseen = set()\nfor tx, ty, raw in layer.iter_tiles():\n    real_gid, flags = decode_gid(raw)\n    if real_gid in seen:\n        continue\n    seen.add(real_gid)\n    ts = tmap.get_tileset_for_gid(real_gid)\n    if ts:\n        lid = ts.global_to_local(real_gid)\n        r, g, b = ts.get_dominant_color(lid)\n        print(f\"GID {real_gid:4d} \u2192 rgb({r},{g},{b})\")\n</code></pre>"},{"location":"guide/pygame_example/#architecture-of-the-example","title":"Architecture of the example","text":"<pre><code>flowchart TD\n    A([main]) --&gt; B[TiledMap.load]\n    B --&gt; C[Camera init]\n    C --&gt; D{Game loop}\n    D --&gt; E[Handle events\\nquit \u00b7 zoom \u00b7 debug]\n    E --&gt; F[Camera scroll\\narrow keys]\n    F --&gt; G[screen.fill bg]\n    G --&gt; H[draw_all_layers\\noffset=camera.offset]\n    H --&gt; I{show_debug?}\n    I -- yes --&gt; J[draw_debug overlay\\nFPS \u00b7 cache stats]\n    I -- no --&gt; K\n    J --&gt; K[pygame.display.flip]\n    K --&gt; L[clock.tick 60]\n    L --&gt; D\n    D -- quit --&gt; M([pygame.quit])</code></pre>"}]}